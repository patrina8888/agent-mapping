# Agent Mapping v3 — Update Memo (2026-09-16)

Three asks from Patrina, answered. Research files in `research/v3_*.md`; scored data in `research/v2_master.json` (now 87 rows); triage in `research/pipeline/`.

---

## ① GPT-6 re-run — **the map barely moves, and that IS the finding**
Constraint-based delta pass (not a full rescore). **Zero integer rescores across the frozen rows.** GPT-6 Astra (shipped ~3 Sep 2026) is incremental: OSWorld-2.0 reliability ~20%→31% (still far below the 80–98% production bar), one minor conviction firming (W2.4 research synthesis), **coding a dead tie**, and it unlocked **zero** BLOCKED/TOO-EARLY cells — every one is gated by regulation, liability, incumbency, or hardware, none of which a smarter model touches. Astra's own cyber-gating actually *tightened* a regulatory gate.
**IC action: do not re-weight a single position on GPT-6.** Keep underwriting the gates that bind (pilot-to-production, R, D) and the coupled-capture-expansion resolvers. Full detail: `research/v3_gpt6_delta.md`.

## ② PhysicalAI — new **first-class family P1–P8** (built)
Re-filed ~$15T of physical labor off *industry* and onto the axis that actually gates robots — **environment-structure × task-physics** — into 8 capability bands. Same dollars, **counted once** (P1–P7 tagged `non_additive`; clean no-double-count proof). Surgical robotics correctly *excluded* to the `[$]` ledger, not inflated.

| Band | Capture today | Read |
|---|---|---|
| **P1** structured-cell (machine tending) | ~1–1.5% | the only place displacement is real — OEM/integrator-owned, not a seed wedge |
| **P7** sensing/inspection/security | <1% forming | **physical mirror of the SOC hunting ground** — Knightscope $7–11/hr vs $25–45/hr guard |
| **P3** open-road AV | <1% | gated by R=5 safety-case, a capex story |
| **P4/P5/P6** unstructured / deformable / human-contact care | ~0% | **hardware-timeline-blocked 5–15yr regardless of model IQ** — $5.3T in P4 must NOT read as addressable |
| **P8** robot-economy labor *(NET-NEW)* | n/a | **the layer the map was missing** — teleop-as-a-service, fleet supervision, commissioning, embodied-data frame-verification. Hardware-decoupled, not-yet-incumbent-captured. **CyberOrigin sits here** — the structural home for the HTC robotics thesis |

ITS ranks confirm the discipline: **P2 (54.6) and P1 (44.0)** rank high (capture real/forming); **P4/P5/P6 land at 22–26**, crushed by the hardware-readiness gate — the giant pools do not masquerade as investable. Detail: `research/v3_physicalai_architecture.md` + `research/v3_pack_P1…P8.md`.

## ③ Completeness + triage — the standing machine (specced; cron pending your OK)
- **Completeness:** map is exhaustive at the family level (tiles ~$60T, all 9 double-count guards hold). Honest debt found: thin named-track coverage of the **in-person-services long tail** → **opened W3.8** (~$3–6T, capture≈0, Tier-B; de-biases the headline). 5 boundary cells staged as `todo` in the coverage ledger. `research/v3_completeness_audit.md`.
- **Triage:** a computable **Investability-Triage Score** (`research/pipeline/triage.py`) that defuses the $2T machine-tending "dollar trap" (readiness gate) and separates *displacement* from *investable* (translation scores high on displacement but W=0, door closed). Raw #1 is **W3.7 Collections** (under-consensus, door wide open); named hunting grounds sit mid-pack — correctly, because investable = high ITS **AND** door-open **AND** a live trigger. Five state-change triggers (T1–T5) are the "surface NEW domains" engine. Full spec incl. cron wiring: `research/v3_pipeline_spec.md`.

---

## What changed in the data/dashboard this cycle
- `v2_master.json`: **78 → 87 rows** (+P1–P8, +W3.8). Added a `non_additive` field to every row (true for P1–P7 + the SA/AO/W7 overlays).
- **Fixed a latent double-count:** the dashboard headline `[W]` total previously summed the non-additive overlays despite the "non-additive" caveat. Now excludes non-additive rows from the `[W]` KPI, the family dollar chart, and the opportunity scatter. **Honest headline [W] = $28.6T** (vs a wrong $44.8T if everything were summed).
- `research/pipeline/`: seeded `state_prev.json` (87 component vectors for diffing), `coverage_ledger.json` (family ledger + 3 enforcement tags + white-cell todos), regenerated `its_ranked.csv`.
- Dashboards (EN + CN): re-inlined data, header now "87 tracks · 18 families incl. first-class PhysicalAI".

## Pending (your call — not done)
- **Create the pipeline cron** (weekly light / monthly re-score / bi-monthly deep → PushNotification digest, human gate; sibling to the daily-brief routine). Specced in `research/v3_pipeline_spec.md §D`; **awaiting your OK on cadence before I create it.**
- Optional follow-ups: a dedicated PhysicalAI capability-band panel in the dashboard; a full BLS-SOC `coverage_ledger` crosswalk (currently family-level).
